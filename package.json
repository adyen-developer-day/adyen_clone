{
  "name": "adyen-clone",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Workshop replica of the Adyen marketing homepage (localhost only).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.1.5",
    "@testing-library/react": "^14.1.2",
    "@vitejs/plugin-react": "^4.2.1",
    "jsdom": "^23.0.1",
    "vite": "^4.5.0",
    "vitest": "^0.34.6"
  }
}
